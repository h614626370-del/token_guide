# Token Guide

VitePress documentation site for the Token向云 member guide.

## Development

```bash
npm install
npm run docs:dev
```

## Build

```bash
npm run docs:build
```

The production files are generated in `dist` and are configured for `/guide/`.

## Deploy

```powershell
.\deploy.ps1 -Key
.\deploy.ps1
```
